# Plaintext shape only. Do not put real preview credentials in this file.
# The `pr-preview` GitHub environment can decrypt the matching SOPS target,
# which contains only the forced-command SSH key.
ci:
  ssh_private_key: |-
    -----BEGIN OPENSSH PRIVATE KEY-----
    replace-with-the-dedicated-portfolio-preview-deploy-key
    -----END OPENSSH PRIVATE KEY-----
